{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tabela creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tabelaPha') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
