{% extends '::base.html.twig' %}

{% block body -%}
    <h1 class="page-header">Tabela Pha letra</h1>

    <table id="table_id" class="table table-responsive table-hover">
        <thead>
            <tr>
                <th>Codigo</th>
                <th>Resultado</th>
                <th>Açoes</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.phaCode }}</td>
                <td>{{ entity.phaName }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% endblock %}
{% block javascripts %}
    <script>
        $(document).ready(function() {
            $('#table_id').dataTable( {
                "paging":   false,
                "ordering": false,
                "info":     false
            } );
        } );
    </script>
{% endblock %}
